<script>
  import Header from '../components/Header.deshi';
  import Sidebar from '../components/Sidebar.deshi';
  import OnThisPage from '../components/OnThisPage.deshi';

  const brand = props.brand || "Deshi";
  const title = props.title || "On this page";
</script>

<!-- STANDALONE 3-COLUMN DOCS LAYOUT -->
<div class="min-h-screen flex flex-col w-full">
  <!-- Mobile Sidebar Checkbox Toggle Controller (0 JS) -->
  <input type="checkbox" id="mobile-sidebar-toggle" class="peer hidden" />

  <!-- Backdrop overlay for mobile sidebar drawer (0 JS) -->
  <label for="mobile-sidebar-toggle" class="fixed inset-0 bg-black/80 backdrop-blur-xs z-50 hidden peer-checked:block lg:hidden cursor-pointer"></label>

  <Header brand={brand} />

  <!-- 3-COLUMN DOCS BODY - 100% FULL WIDTH EDGE-TO-EDGE -->
  <div class="flex-1 flex w-full">
    <Sidebar brand={brand} />

    <!-- CENTER MAIN CONTENT -->
    <main class="flex-1 min-w-0 w-full">
      <div class="mx-auto px-4 pt-6 pb-12 lg:px-6 max-w-(--nb-content-max)">
        <slot />
      </div>
    </main>

    <OnThisPage title={title} />
  </div>
</div>
