{
  "name": "deshi-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Deshi monorepo — the publishable `deshi` compiler package and an example site that consumes it.",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "trustedDependencies": [
    "esbuild"
  ],
  "scripts": {
    "build": "bun run --filter deshijs build && bun run --filter deshi-playground build",
    "build:compiler": "bun run --filter deshijs build",
    "dev": "bun run build:compiler && bun run --filter deshi-playground dev",
    "preview": "bun run --filter deshi-playground preview",
    "test": "bun run --filter deshijs test",
    "test:watch": "bun run --filter deshijs test:watch",
    "typecheck": "bun run --filter deshijs typecheck && bun run --filter deshi-playground typecheck"
  },
  "engines": {
    "node": ">=18"
  }
}
